<hibernate-configuration>

    <session-factory>
        <!-- Database connection settings -->
        <property name="hibernate.connection.driver-class">com.mysql.cj.jdbc.Driver</property>
        <property name="hibernate.connection.url">jdbc:mysql://localhost:3306/janbatch</property>
        <property name="hibernate.connection.username">root</property>
        <property name="hibernate.connection.password">system</property>

        <!-- SQL Dialect-->
        <property name="hibernate.dialect">org.hibernate.dialect.MySQLDialect</property>

        <!-- Show SQL queries in console -->
        <property name="hibernate.show_sql">true</property>
        <property name="hibernate.format_sql">true</property>

        <mapping class="com.kodnest.OneToMany.Employee"/>
        <mapping class="com.kodnest.OneToMany.Department"/>

    </session-factory>
</hibernate-configuration>